# Release 0.5.0 — Triage + Drift Audit + Capability Experiment

- Date (UTC): 2026-09-15 · Branch: `dev` @ `ce16f10` · Last tag: `v0.4.1`
- pyproject in-tree: `0.4.1` → release target `0.5.0` (minor: Timezone + UtcOffset are new capabilities; no semver mismatch)
- Python 3.13.5 · `uv lock --check`: clean · Labels: `v0.5.0` (v-prefixed convention)
- Scratch: `/tmp/opencode/release-0.5.0/` (`run_experiment.py`, SEED `20260915`; fixed row order) — harness scratch used instead of `tmp/` per environment; no `.gitignore` edit needed

## § Triage (Phase 1)

Open labelled issues at start: #154 (flake, bug), #146 (backlog tracker). Closed-since-`v0.4.1` labelled issues feeding notes: #15, #71, #73, #106, #107, #132, #135, #137, #144, #145, #147, #148, #149, #150, #161, #162, #165.

| Issue | Verdict | Evidence | Action |
|-------|---------|----------|--------|
| #154 flake | defer (unreproduced) | 20+ consecutive combined `tests/property + tests/e2e/test_cli_scan.py` green loops on file; zero code changed (Slice E) | `v0.5.0` label removed 2026-09-15 with evidence comment; kept open; listed under Known limitations (user-confirmed) |
| #146 tracker | out of scope (not one task) | body is a candidate collection for future scheduling | `v0.5.0` label removed 2026-09-15 with comment; kept open (user-confirmed) |

Result: **zero open `v0.5.0` issues** (`gh issue list --state open --label v0.5.0` → empty).

## § Drift Audit (Phase 2)

Post-#166 tree. Checks use the updated skill (checks 1–11).

| # | Check | Result |
|---|-------|--------|
| 1 | README table vs `generate_readme_table.py` + `list_shipped_capabilities()` | PASS — 20 rows byte-identical; 20 names alphabetical |
| 2 | CONTEXT.md entries | PASS — Timezone/UtcOffset rows + tree entries present |
| 3 | docs/user accuracy + Status-table pins | PASS — GMT SUCCESS + `+05:60` MISSING rows present (live-verified) |
| 4 | Docstring drift | PASS — `test_docstring_presence.py` green; `pyright` 0 errors; Timezone/UtcOffset + orchestrator qualification docstrings verified |
| 5 | Forbidden refs | PASS — `grep -rn docs/development` over all shipped surfaces → empty |
| 6 | ADR statuses | PASS — all Accepted/Obsolete consistent; 0012 Accepted 2026-09-10; 0009 carries v0.5.0 historical-count note |
| 7 | Links + counts | PASS — Starlight-aware link check: none broken; remaining count strings dispositioned (ARCHITECTURE/CONTRIBUTING "four layers" = architectural, correct; migration "18 shipped" = historical 0.4.0, correct) |
| 8 | Packaging | PASS — `py.typed` ships in wheel; version `0.4.1` pre-bump (plan task) |
| 9 | CHANGELOG Unreleased | PASS — covers Timezone/UtcOffset + all slices (finalize to `0.5.0` in release PR) |
| 10 | Versioned snapshot | N/A pre-release — no `v0.5.0` entry yet (release-PR task per plan) |
| 11 | New-cap checklist | PASS — Timezone/UtcOffset present on all 12 surfaces (guide, chooser, concepts, api-reference ×3, citations, glossary, migration, index, README, CONTEXT, AGENTS) |

No new drift findings. One P2 observation carried from the experiment (E3 below).

## § Experiment (Phase 3)

Scope: **full 20-capability set** — `git diff --stat v0.4.1..HEAD -- paxman/` touches `paxman/core/grammar/` (boundary_spec, combinator, scanner, normalizers, stages) and `paxman/engine/orchestrator.py`, so every capability is in scope. Corpus: **123 rows** (6 per capability; Money 7, UtcOffset 8), states valid-canonical / valid-variant / structurally-invalid / semantically-invalid / absent / two-mentions, run twice via in-tree `uv run python` + `register_all_shipped()`.

- Determinism: `cmp run1.json run2.json` → **byte-identical**.
- Mismatches: **2/123**, both adjudicated oracle errors (not code findings):
  - E1 (coordinates `"48.5, 2.3 EPSG:3857"` → SUCCESS span `(0,9)`): guide's foreign-CRS INVALID applies to `geo:` URIs with `crs=` (`coordinates.md:87`), not bare pairs with trailing prose. Oracle corrected.
  - E2 (si_unit `"kg and m"` → AMBIGUOUS): `single_value` defaults False (`domain.py:295`); SIUnit grammars don't opt in, so no `MultipleMentionsError` by design (ADR-0004). Oracle corrected.
- E3 (P2 docs-precision, pre-existing): `pipeline.md:109` states the single-mention invariant unconditionally; engine enforces it only for `single_value=True` grammars. Recommend one qualifying sentence (plan task, non-blocking).
- Distribution: every capability has non-SUCCESS rows; 22 INVALID total; AMBIGUOUS covered (Date, IP, SI Unit) + 19 MME rows.
- Provenance: all SUCCESS rows carry authority data (none missing).
- Re-entry: 12/12 SUCCESS values re-enter under default contracts.
- CLI parity: 12/12 `--json` rows match API (`email date country timezone utcoffset isbn issn phone url money language currency`); `--list` shows all 20; stdin + invalid-input (`MISSING`) verified.
- Cross-capability confusion: N/A by design (one contract per row).

## § Gates (Phase 5)

| Gate | Result |
|------|--------|
| G1 quality | PASS (CI scope): `ruff check paxman/ tests/` + `ruff format --check` clean; `pyright` 0 errors; `import-linter` kept. Pre-existing notes (not blockers): 3× E501 in `tools/regenerate_isbn_range_data.py` / `regenerate_idna_uts46_data.py` (untouched since ≤v0.4.1; CI doesn't scope `tools/`); `learnings/` ruff debt (out of scope) |
| G2 coverage | PASS — 95.79% ≥ 95 (5168 passed, 2 skipped) |
| G3 benchmark | PASS with note — two runs uniformly +5–46% across ALL scenarios incl. `freeze` (+29%) vs `baseline.json`; uniform uplift = box noise, no scenario-specific regression signature. Recommend re-run on quiet CI runner; non-blocking |
| G4 build/wheel | PASS — `py.typed` ships; no `tests/`/`development`/`tmp/`; throwaway-venv `--list` shows 20 caps; `timezone US/Eastern` smoke SUCCESS (artifacts built at 0.4.1 pre-bump) |
| G5 CLI | PASS — 12-row parity + `--list` + stdin + invalid (see Experiment) |
| G6 publish | PASS logic — tag↔version parity enforced (`publish.yml:28-40`); requires pyproject `0.5.0` before tagging `v0.5.0` (plan task) |
| G7 deps | PASS — `uv lock --check` clean |
| G8 docs build | PASS — `npm run build` exit 0, 170 pages (pre-snapshot; re-run after `v0.5.0` snapshot in release PR) |

No P0 findings. Open items for the plan: E3 (P2), #154 known-limitation note, release mechanics (bump, CHANGELOG finalize, `v0.5.0` snapshot + G8 re-run, tag/publish).
