# Release 0.5.0 readiness plan

| Field | Value |
|-------|-------|
| Title | v0.5.0 release readiness — Timezone/UtcOffset + slices A–H |
| Date | 2026-09-15 |
| Status | Draft — for review |
| Branch | `release/0.5.0` (worktree per convention) |
| Findings source | `docs/development/reports/2026-09-15-release-0.5.0-experiment.md` |
| Release target | `0.5.0` |
| Related | `docs/development/reports/2026-09-15-release-notes-0.5.0.md` |

## Findings → task mapping

| Finding | Severity | Task |
|---------|----------|------|
| Triage #154, #146 | done | Labels removed with evidence comments 2026-09-15; #154 listed as known limitation |
| Drift audit | clean | No tasks — post-#166 tree passes checks 1–11 |
| E1, E2 experiment | oracle corrections | No tasks (guide-consistent behavior, report § Experiment) |
| E3 pipeline.md opt-in wording | P2 | T1: qualify single-mention sentence with `single_value` opt-in |
| G1 tools/ E501 ×3 | P2 (pre-existing) | T2: fix line lengths in `regenerate_isbn_range_data.py:102,129`, `regenerate_idna_uts46_data.py:149` |
| G3 benchmark noise | note | Re-run on quiet CI runner; non-blocking |
| Release mechanics | P1 | T3–T6: version bump, CHANGELOG finalize, `v0.5.0` snapshot + G8, AGENTS version line |

## Progress

| Task | Status | Commit |
|------|--------|--------|
| T1 pipeline.md opt-in sentence | todo | — |
| T2 tools/ E501 | todo | — |
| T3 `pyproject.toml` 0.4.1 → 0.5.0 | todo | — |
| T4 CHANGELOG `Unreleased` → `0.5.0` + date | todo | — |
| T5 `docs/user/` → `docs/user/v0.5.0/` snapshot (slug overrides + `versions.json` + `docs_site/src/content/versions/v0.5.0.json`) + G8 re-run | todo | — |
| T6 tag `v0.5.0` → publish → PyPI verify → GitHub Release | todo | — |

## Tasks

- [ ] **T1** — `docs/user/concepts/pipeline.md:109`: append opt-in qualification ("enforced for grammars opting in via `Grammar.single_value`; otherwise distinct values read `AMBIGUOUS`"). Verify: `grep -n single_value docs/user/concepts/pipeline.md`.
- [ ] **T2** — wrap 3 long lines in `tools/`. Verify: `uv run ruff check tools/`.
- [ ] **T3–T5** — release mechanics on `release/0.5.0` (single PR with T1+T2): bump, CHANGELOG date (`2026-09-15`), snapshot per skill check 10, `npm run build` green with `dist/v0.5.0/`.
- [ ] **T6** — ship checklist: merge → tag `v0.5.0` (parity gate enforces `pyproject.toml` match) → publish workflow → verify PyPI → GitHub Release from finalized notes.

## Decisions

None pending (triage dispositions #154/#146 user-confirmed 2026-09-15).

## Ship checklist

See T6. Post-tag: confirm `versions.json` serves `/v0.5.0/` routes on the deployed site.
