# Slice D — Audit Closeout (#147) + Missing Guides (#145)

> **For workers:** Execute task-by-task via `tdd` + `verification-before-completion`. Review gate: `paxman-momus-review` (plan), `paxman-oracle-review` (after impl). DO NOT implement until explicitly told.

**Goal:** Close #147 fully (B6 display fidelity, B7 synthetic hygiene, D2 README sync, D5 stub removal) and finish the user-guide set (BIC, Coordinates, Element, IBAN, ORCID), leaving #145 closable.

**Architecture:** Display-form companion table (rules/data, IANA-sourced) consulted before the `title()` fallback; dead generated outputs removed at the generator (not just the files); README row regenerated by its tool; guides mirror the `mac_address.md` precedent. No grammar/rule-logic, contract, engine, or core changes.

**Tech Stack:** Python 3.11+, uv, ruff, strict pyright, import-linter, pytest, `tools/regenerate_iana_language_data.py`, `tools/generate_readme_table.py`.

**References:** Issues #147 (B6/B7/D2/D5), #145; `paxman/capabilities/Language/capability.py:55-58,260-271` (reverse map + title fallback); `paxman/shared_data/language_snapshot.json` (`name_to_canonical`, normalized); `tools/regenerate_iana_language_data.py:26-36,97-101,149-150`; `tests/unit/test_derived_keys.py:20-24`; `tools/generate_readme_table.py` (usage block); `docs/user/capabilities/mac_address.md` (guide template); `docs/user/capabilities/language.md:11-175` (section structure).

**Branch:** `feature/slice-d-closeout-guides` (cut from `dev` — already exists).

---

## File Structure

- Create: `paxman/capabilities/Language/rules/data/english_display_names.py` — hand-maintained code→display companion (B6).
- Modify: `paxman/capabilities/Language/capability.py:260-271` — consult companion before `title()` fallback (B6).
- Modify: `tools/regenerate_iana_language_data.py` — drop `iana_registry.py` + `names.py` emissions (B7/D5).
- Delete: `paxman/capabilities/Language/grammar/data/iana_registry.py`, `names.py` (B7/D5, via generator + regen).
- Modify: `tests/unit/test_derived_keys.py:20-24` — cover real key sources (B7).
- Modify: `README.md:84` — regenerate row via tool (D2).
- Create: `docs/user/capabilities/bic.md`, `coordinates.md`, `element.md`, `iban.md`, `orcid.md` (Task 2; index rows by integrator).
- Docs: `CHANGELOG.md`.
- No grammar/rule/contract/engine/core logic changes.

---

## Background the implementer needs

### Current state (verbatim)

B6 — reverse map is normalized-only (`capability.py:55-58`):

```python
_CANONICAL_TO_ENGLISH: dict[str, str] = {v: k for k, v in NAME_TO_CANONICAL.items()}
```

and the snapshot itself stores stripped keys (`"norwegian bokmal"`, `"cesky"`, `"espanol"`), so the true display forms (`Bokmål`) exist nowhere shipped; `raw.title()` (`:268-270`) then cements the loss. Fix = companion display table (data change, exactly as #147 prescribes).

B7/D5 — generator pads to 100 (`regenerate_iana_language_data.py:97-101`):

```python
    # Ensure >77: if snapshot subset is exactly 77, pad with synthetic
    synthetic_needed = max(0, 100 - len(keys))
```

`names.py` has exactly one consumer (`test_derived_keys.py:22`, asserting `> 77` — true only because of padding); `iana_registry.py` has zero (only its generator references it). Both are dead weight in shipped data.

D2 — `README.md:84` claims `3 (bcp47_tag, language_code, language_name) | 10`, but Slice B shipped a 4th grammar + 11th rule; the table is generated by `tools/generate_readme_table.py` from live capabilities, so the fix is re-running it (plus checking whether its `_DESCRIPTIONS`/caveat path covers the subset disclaimer).

### Design decisions (locked)

1. **B6 = companion table, seeded minimal.** `english_display_names.py` (`{"nb": "Norwegian Bokmål", ...}` — seed with the reported vector; IANA-sourced header; curated-subset + extension rule documented). Other diacritic-losers cannot be enumerated without an external source, so the table starts at Bokmål rather than guessing. Consulted in `format_value` between the map hit and the `title()` fallback (fallback stays for unlisted codes).
2. **B7/D5 = delete at the generator, not just the files.** Remove both emissions from `tools/regenerate_iana_language_data.py`, regenerate, prove all other outputs byte-identical (`git diff` shows only the two deletions), rewrite `test_derived_keys.py` to assert on the real key sources (`ENGLISH_LANGUAGE_KEYS`/`LOCALIZED_LANGUAGE_KEYS` derivation + no `synthetic` key anywhere in shipped grammar data). Fallback if regen diffs unexpectedly: keep `names.py` unpadded instead — do not hand-edit generated files either way.
3. **D2 = tool output, verified.** Run `uv run python tools/generate_readme_table.py`, replace the Language row, re-run to prove idempotence; subset-caveat wording lives wherever the tool supports it (`_DESCRIPTIONS` or adjacent prose — implementer checks, never hand-edits generated table body).
4. **Guides mirror `mac_address.md`, one capability each.** Sections: plain-language intro, recognizes/does-not table, canonical output + `output_format` table, contract block, statuses table, executed notebook snippet, provenance, see-also footer. Every snippet executed via `uv run python` before finalizing; no `index.md` edits (integrator does all four rows at once to avoid conflicts); no capability-code changes for docs.
5. **#15/#150 code stays out** (unratified rulings); #71.3-4/#73 L3 stay out (design-gated); #107 docstrings stay out (non-gated hygiene).

### Deferral → issue map

| Deferred matter | Home issue |
|---|---|
| Broader display-name companion entries | #147 (extension rule in file header) |
| Broader script/region tables, paren follow-ups | #148 (open) |
| #107 docstrings, #154, #15/#150 code, #71.3-4, #73 L3 | Their issues, untouched |

---

### Task 1: #147 closeout — B6 display table, B7/D5 generator surgery, D2 row

**Files:** per File Structure (capability.py, english_display_names.py create, generator, 2 deletions, test_derived_keys.py, README.md:84)

**Goal:** `format_value("nb", "name")` → `Norwegian Bokmål`; dead generated data gone; README row true.

- [ ] Failing tests first: `test_name_orthography` (`de`→`German` unchanged + `nb`→`Norwegian Bokmål` FAILS today as `Norwegian Bokmal`); derived-keys rewrite (no `synthetic` in shipped data; real-source assertions FAIL on current tree). Run: targeted → FAIL.
- [ ] Implement: companion table + fallback consultation; generator emission removal + regen (prove byte-identical others); test rewrite; README regen + idempotence check. Verify: `uv run pytest tests/capabilities/language tests/unit/test_derived_keys.py -q` → PASS; `uv run python tools/regenerate_iana_language_data.py --check`-equivalent drift guard green (whichever flag the tool offers — read `--help` first); ruff/pyright clean.

### Task 2: #145 — four missing guides (BIC, Coordinates, Element, IBAN, ORCID)

**Files:** one new `.md` per capability under `docs/user/capabilities/` (no index edits, no code)

**Goal:** Each shipped-without-guide capability gets the full 7-section guide with executed snippets.

- [ ] Per guide (4 parallelizable units): read the capability's `contract.py` (DEFAULT/OFFERED formats), `capability.py` (`format_value`), `grammar/` shapes, `rules/` sections + provenance, existing tests for edge vectors; draft against `mac_address.md`; execute every snippet via `uv run python` and fix wording to observed output. Acceptance per guide: all 7 sections present, snippets green, provenance cites match code `PUBLICATION`s, statuses table covers MISSING/INVALID + format-error paths.
- [ ] Verify: docs diff only (no test gate applies to prose); integrator re-runs any snippet on demand.

### Task 3: Integrate + gate + close-out

**Files:** `docs/user/capabilities/index.md` (4 rows), `CHANGELOG.md`

- [ ] Index rows `BIC/Coordinates/Element/IBAN/ORCID`: `[Name](slug/)` replacing forthcoming stubs; verify links resolve (filenames match slugs). CHANGELOG Unreleased/Fixed: #147 closeout + 4 guides. Gate: `uv run ruff check paxman/ tests/ && uv run ruff format --check paxman/ tests/ && uv run pyright && uv run import-linter lint && uv run pytest` → green; coverage ≥95. Close-out comments: #147 (all five items with evidence), #145 (all guides linked — closable). Commit per task.
