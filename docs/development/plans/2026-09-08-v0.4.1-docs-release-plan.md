# v0.4.1 Docs Correctness + Patch Release Plan (issue #142)

> **For workers:** Execute task-by-task via `tdd` + `verification-before-completion`. Review gate: `paxman-momus-review` (plan), `paxman-oracle-review` (after impl).

**Goal:** Fix the verified docs inaccuracies from the 0.4.0 release review (issue #142 checklist) and ship patch `0.4.1` (docs-only, no contract changes).

**Architecture:** Docs-only release. No `paxman/`, `tests/`, `benchmarks/` changes — so no TDD red/green cycle applies (no behavior to pin); each task verifies with targeted `grep`/`uv run` doc-example checks plus the standard gates. Fix live `docs/user/**` first, then snapshot `docs/user/v0.4.1/`, bump `pyproject.toml` `0.4.0→0.4.1`, stamp `CHANGELOG.md`, append `versions.json`. Branch from updated `dev`; PR to `dev`; then `dev`→`main` + tag `v0.4.1`. What is NOT changed: 7 missing guide pages, segmentation links, citations example, pipeline doc (all decided in #142).

**Tech Stack:** Python 3.11+, uv, ruff, strict pyright, import-linter, pytest (95% floor), `gh` CLI, `paxman` pipeline for example verification.

**References:** Issue #142, `paxman/capabilities/Date/rules/en_50160_ed2010.py:1-14` (attribution retraction), `paxman/capabilities/Date/rules/iso_8601_ed2019.py` (`Section 5.2.1.1-calendar-date`), `paxman/api/bootstrap.py:65-88` (tuple return), `paxman/api/canonicalize.py:9-31` (raises), `paxman/core/domain.py:37-46` (7 Provenance fields), `paxman/core/domain.py:222-233` (VersionStamp), `paxman/capabilities/Country/capability.py:76-86` (factory flags), `paxman/capabilities/__init__.py:61-82` (factory names), `docs_site/versions.json`, `docs/user/v0.3.2/index.md:1-5` (slug pattern).

**Branch:** `docs/v0.4.1-release-followups` (split from updated `dev` @ `913226e` + main merge no-op)

---

## File Structure

- Modify: `docs/user/capabilities/date.md` — diagram rule labels + provenance paragraph + concrete pinned rule + provenance-loop setup
- Modify: `docs/user/concepts/candidates-and-ambiguity.md` — diagram rule labels + pinned rule name + MISSING definition + 3 import fixes
- Modify: `docs/user/api-reference.md` — `register_all_shipped` signature + `canonicalize` CapabilityError row + quick-lookup 8 rows
- Modify: `docs/user/concepts/provenance.md` — six→seven fields
- Modify: `docs/user/concepts/execution-result.md` — VersionStamp both fields
- Modify: `docs/user/concepts/contracts.md` — add `suppress_common_words` row
- Modify: `docs/user/concepts/capabilities.md` — add 8 capability rows
- Modify: `docs/user/capabilities/index.md` — add 8 chooser rows
- Modify: `docs/user/capabilities/email.md` — obfuscated comment + provenance setup
- Modify: `docs/user/capabilities/issn.md` — `-\?`→`-?`
- Modify: `docs/user/capabilities/currency.md` — drop casing claim
- Modify: `docs/user/extending.md` — 2 import fixes + illustrative provenance reframe + note
- Modify: `docs/user/getting-started.md` — `text` fence
- Modify: `docs/user/glossary.md` — factory fix
- Modify: `pyproject.toml`, `CHANGELOG.md`, `docs_site/versions.json`, `uv.lock` (via `uv lock` if needed)
- Create: `docs_site/src/content/versions/v0.4.1.json`, `docs/user/v0.4.1/**` (snapshot)

No `paxman/` edits. No `docs/development/` references from shipped docs.

---

### Task 1: Date false-provenance corrections

**Files:** `docs/user/capabilities/date.md`, `docs/user/concepts/candidates-and-ambiguity.md`

**Goal:** Docs cite only rule names/authorities that exist in code.

- [ ] `date.md` diagram: `Section 4.3.1-calendar-date`→`Section 5.2.1.1-calendar-date`, `Section 1-date-format`→`Derived-US-date-format`, `Section 4-date-format`→`Derived-European-date-format`. Table row `pinned_rules=["…calendar-date"]`→`pinned_rules=["Section 5.2.1.1-calendar-date"]`. Provenance paragraph: ISO 8601 `Section 5.2.1.1-calendar-date`; US/European as derived conventions (no EN 50160). Provenance loop: prepend `import paxman` / `from paxman.capabilities import Date` + `result = paxman.canonicalize("2026-01-15", Date.create_contract())`.
- [ ] `candidates-and-ambiguity.md` diagram: `Section 1-date-format (US federal)`→`Derived-US-date-format (derived convention)`; `Section 4-date-format (CENELEC EN 50160)`→`Derived-European-date-format (derived convention)`. Pin example: `Section 4.3.1-calendar-date-us`→`Derived-US-date-format`.
- [ ] Verify: `grep -rn "4\.3\.1-calendar-date\|Section 1-date-format\|Section 4-date-format\|EN 50160" docs/user/capabilities/date.md docs/user/concepts/candidates-and-ambiguity.md` → 0 hits. Run `uv run python -c` pinning `Derived-US-date-format` on `01/02/2026` → SUCCESS (US reading). Commit: `docs(v0.4.1): correct Date rule citations (#142)`.

### Task 2: API reference + concept corrections

**Files:** `docs/user/api-reference.md`, `docs/user/concepts/provenance.md`, `docs/user/concepts/execution-result.md`, `docs/user/concepts/contracts.md`, `docs/user/concepts/candidates-and-ambiguity.md`

**Goal:** Signatures, counts, tables match code.

- [ ] api-reference: `` `() -> None` ``→`` `() -> tuple[str, ...]` `` + "Returns names newly registered" for `register_all_shipped`; `CapabilityError` canonicalize row → "No capability matches `contract.capability_name`" only.
- [ ] provenance.md: "six fields"→"seven fields"; keep section-citation sentence (change "seventh piece"→"separate piece" if needed for consistency).
- [ ] execution-result.md: `VersionStamp(paxman_version="…")`→`VersionStamp(paxman_version="…", recognition_revision="…")`.
- [ ] contracts.md table: add `` `suppress_common_words` | `bool` | `False` | Suppress word-bounded short-code hits in `COMMON_WORDS` (67) `` row.
- [ ] candidates-and-ambiguity.md: MISSING bullet → "no grammar matched, or a recognized match was suppressed (`suppressed_count > 0` — see [Execution Result](execution-result/))"; add `import paxman` to pin block, `import paxman` + Date import to surface block (`from paxman.capabilities import Date` already in pin block — reuse pattern).
- [ ] Verify: `grep -rn "six fields\|() -> None.*[Rr]egister\|No capability matches.*duplicate" docs/user/api-reference.md docs/user/concepts/provenance.md` → 0 hits. Commit: `docs(v0.4.1): align API/concept docs with code (#142)`.

### Task 3: Snippet and row polish

**Files:** `email.md`, `issn.md`, `currency.md`, `extending.md`, `getting-started.md`, `glossary.md`

**Goal:** Every touched snippet runnable as written; no false claims.

- [ ] email.md: comment → `include_obfuscated=False,  # bool, default False — set True to recognize "user at example dot com"`; provenance block prepend `import paxman` / `from paxman.capabilities import Email` / `result = paxman.canonicalize("user@Example.COM", Email.create_contract())`.
- [ ] issn.md: `` `-\?` ``→`` `-?` ``.
- [ ] currency.md: `Codes with wrong length or unsupported casing (`US`, `USDD`)`→`Codes with wrong length (`US`, `USDD`)`.
- [ ] extending.md: add `import paxman` + `from paxman.capabilities import Date` to freeze block; add `from paxman.capabilities import Date` to opt-in block; DotDateRule `Provenance(authority="ISO", specification_name="ISO 8601", …, version="2019", publication_year=2019)` + `citation = "Section 4.3.1 (calendar date)"` → illustrative derived-convention provenance (`authority="Example"`, `specification_name="Dot-date convention (illustrative)"`, `reference_url=""`, `version=None`, `publication_year=2026`, `citation="Illustrative only — not an ISO 8601 citation"`) + one sentence after the class noting the provenance is illustrative.
- [ ] getting-started.md: output fence ```` ``` ````→```` ```text ````.
- [ ] glossary.md: `` `Country(include_historical=True)` ``→`` `Country.create_contract(include_historical=True)` ``.
- [ ] Verify by executing each touched snippet: `uv run python - <<EOF` per block (email provenance loop, extending opt-in import shape via `python -c "import ast"` at minimum; full extending lifecycle run is opt-in — run it: register grammar+rule in-process and canonicalize `2024.01.01` → `2024-01-01`). Commit: `docs(v0.4.1): fix snippets and rows (#142)`.

### Task 4: 18-capability inventory completeness

**Files:** `docs/user/capabilities/index.md`, `docs/user/concepts/capabilities.md`, `docs/user/api-reference.md`

**Goal:** All 18 shipped capabilities discoverable from all three inventories.

- [ ] Add rows for BIC, Coordinates, Element, IBAN, ISSN, Language, MacAddress, ORCID with one-line descriptions from `CONTEXT.md`/CHANGELOG (BIC ISO 9362 business identifiers; Coordinates WGS 84; Element IUPAC symbols; IBAN ISO 13616; ISSN ISO 3297 serials; Language BCP 47 tags; MacAddress IEEE 802; ORCID ISO 27729 researcher IDs). Factories: `BIC`/`Coordinates`/`Element`/`IBAN`/`ISSN`/`Language`/`MacAddress`/`ORCID` + `.create_contract(...)`. Link guides where they exist (`issn/`); others link to `../api-reference/` (index) or plain bold (concepts table has no link column).
- [ ] Verify: `uv run python -c "from paxman.api.bootstrap import list_shipped_capabilities as f; print(len(f()))"` → 18; each of the 18 names lowercased appears in all three files (`python3` loop assert). No broken relative links (every `(xxx/)` target exists under `capabilities/`). Commit: `docs(v0.4.1): list all 18 capabilities in inventories (#142)`.

### Task 5: Patch release stamp + snapshot + gates + PR + tag

**Files:** `pyproject.toml`, `CHANGELOG.md`, `docs_site/versions.json`, `docs_site/src/content/versions/v0.4.1.json`, `docs/user/v0.4.1/**`

**Goal:** Green `0.4.1` tag on `main`.

- [ ] `pyproject.toml` `0.4.0`→`0.4.1`; `uv lock --check` (fix via `uv lock` if needed). `CHANGELOG.md`: insert `## [0.4.1] - 2026-09-08` with `### Fixed` docs entries (no Breaking/Added — docs-only patch).
- [ ] Snapshot: copy `v0.3.2.json`→`v0.4.1.json`; append `versions.json`; `rsync` live docs→`docs/user/v0.4.1/` (exclude `v0.*`) + slug injection (`v0.4.1[/<path>]`); `grep -rL "^slug:"` → empty.
- [ ] Gates: `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run import-linter lint && uv run pytest -q` + per-package coverage ≥95 + all `regenerate --check` green.
- [ ] Oracle `paxman-oracle-review` on `dev...HEAD` → PASS required before PR. PR to `dev`; wait for CI + CodeRabbit; address; merge when green; `dev`→`main`; tag `v0.4.1`; push tag.

---

## Decisions

- Patch (not minor): docs-only, zero contract/behavior changes.
- Live docs fixed first, snapshot mirrors them (v0.4.0 snapshot stays frozen — its inherited issues are historical record, consistent with v0.3.2 precedent).
- 7 missing guide pages are a separate follow-up issue, not this release.
- Pipeline `include_localized` CodeRabbit finding verified invalid (doc already correct) — no change.
