# Release 0.4.0 (minor) Implementation Plan (v0.4.0 label)

> **For workers:** Execute task-by-task via `tdd` + `verification-before-completion`. Review gate: `paxman-momus-review` (plan), `paxman-oracle-review` (after impl).

**Goal:** Ship minor `0.4.0` incorporating all `main` (v0.3.2 hotfix line) + `dev` (Element, Coordinates, ADR-0010, ADR-0011 Phases 0-4, suppression A0) changes with docs/version stamp consistent and CI green.

**Architecture:** Release-only chore, no `paxman/` behavior change. Stamp `pyproject.toml` `0.3.2→0.4.0`, promote `CHANGELOG.md` `[Unreleased]`→`[0.4.0]`, fix shipped-doc drift (counts, migration, README table), snapshot versioned docs `docs/user/v0.4.0/` + `docs_site/versions.json` + `docs_site/src/content/versions/v0.4.0.json` with `slug:` injection (v0.3.2 incident). Branch from updated `dev`; PR to `dev`; then `dev`→`main` + tag `v0.4.0`. What is NOT changed: no grammar/rule/contract/engine logic, no new capability, no open `v0.4.0` audit issues (deferred).

**Tech Stack:** Python 3.11+, uv, ruff, strict pyright, import-linter, pytest (95% floor), `gh` CLI, `git`, Astro docs build (`docs_site/`).

**References:** `pyproject.toml:7` (`version = "0.3.2"`), `CHANGELOG.md:9` (`## [Unreleased]`), `paxman/api/bootstrap.py` (`_SHIPPED` 18 caps), `tools/generate_readme_table.py`, `docs_site/versions.json`, `docs_site/src/content/versions/v0.3.2.json`, `docs/user/v0.3.2/index.md:1-5` (slug pattern), `docs/user/migration.md:178` (0.4.0 A0 section), `docs/user/citations.md:5` (stale "15"), `.github/workflows/ci.yml` (merge-blocking), `.github/workflows/publish.yml` (tag↔version check), tags `v0.3.2`, `v0.3.1`.

**Branch:** `chore/v0.4.0-release` (split from updated `dev` @ `b2aa474` + main merge no-op)

---

## File Structure

- Modify: `pyproject.toml` — `version = "0.4.0"`
- Modify: `CHANGELOG.md` — `## [Unreleased]` → `## [0.4.0] - 2026-09-08`, keep Keep-a-Changelog categories
- Modify: `docs/user/citations.md` — "15 shipped" → "18 shipped"
- Modify: `docs/user/migration.md` — extend `0.4.0` section: Phone `national→split`, Language `alpha2` carry, Coordinates new-cap, MacAddress `bit_reversed` de-offer (already in CHANGELOG Breaking; migration must mirror)
- Modify: `README.md` — only if `tools/generate_readme_table.py` diff non-empty (Phone grammars still 4 incl. `national` recognition; `split` is output-only so table likely unchanged — verify, do not hand-edit)
- Modify: `docs_site/versions.json` — append `{"slug":"v0.4.0","label":"v0.4.0","tag":"v0.4.0"}`
- Create: `docs_site/src/content/versions/v0.4.0.json` — copy of `v0.3.2.json` shape (sidebar slugs unchanged)
- Create: `docs/user/v0.4.0/**` — snapshot of `docs/user/*.md`, `capabilities/*.md`, `concepts/*.md` with `slug: v0.4.0[/<path>]` frontmatter injection (mirror `v0.3.2` layout)
- Modify: `uv.lock` — only via `uv lock` if version bump invalidates it (check `uv lock --check`)

No `paxman/`, `tests/`, `benchmarks/` changes. No `docs/development/` references from shipped docs.

---

### Task 1: Branch + version stamp + CHANGELOG

**Files:** `pyproject.toml:7`, `CHANGELOG.md:9`, `uv.lock`

**Goal:** `dev` updated with `main`, release branch cut, version stamped.

- [ ] `git checkout dev && git fetch origin --prune && git merge --ff-only origin/main` → Expected: Already up to date (dev contains main via `f4e850b`, `096e340`; record SHA). Then `git checkout -b chore/v0.4.0-release`.
- [ ] Edit `pyproject.toml` `version = "0.3.2"` → `"0.4.0"`. Run `uv lock --check` → if fail, `uv lock` and include `uv.lock` diff.
- [ ] Edit `CHANGELOG.md`: `## [Unreleased]` → `## [0.4.0] - 2026-09-08`. No content rewrite (Unreleased body already covers Element/Coordinates/suppression/Breaking/Phone-split/Language-carry/Coordinates-fixes). Verify `head -n 15 CHANGELOG.md`.
- [ ] Verify: `uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['version'])"` → `0.4.0`; `uv run ruff check paxman/ tests/ && uv run ruff format --check paxman/ tests/` → PASS. Commit: `chore(release): bump to 0.4.0 + stamp CHANGELOG`.

### Task 2: Docs drift fixes (shipped docs only)

**Files:** `docs/user/citations.md:5`, `docs/user/migration.md:178-`, `README.md` (conditional)

**Goal:** Zero stale counts; migration mirrors CHANGELOG Breaking.

- [ ] `docs/user/citations.md:5`: "15 shipped capabilities" → "18 shipped capabilities". Verify `grep -rn "15 shipped" docs/user/ README.md CONTEXT.md` → 0 shipped-doc hits (glossary historical "15 shipped in v0.2.0" is intentional, leave).
- [ ] `tools/generate_readme_table.py` → diff against `README.md`: `uv run python tools/generate_readme_table.py > /tmp/opencode/readme_table.txt && diff` (or tool's `--check` if present). If diff non-empty, apply generator output; if empty, no README edit with evidence note.
- [ ] `docs/user/migration.md`: extend `## 0.4.0` section to cover all CHANGELOG Breaking rows (Phone `national→split` with `split` example, MacAddress `bit_reversed` removal, Language `alpha2` carry vs `alpha3` waiver, Coordinates new-cap note). Each row: Before/After + Migrate line. Keep A0 table intact.
- [ ] Verify: `grep -rn "docs/development" docs/user/ README.md CONTEXT.md pyproject.toml paxman/` → 0 hits; `uv run pytest tests/unit/test_capability_exports.py -q` → PASS. Commit: `docs(release): fix 0.4.0 drift — citations count, migration completeness`.

### Task 3: Versioned docs snapshot v0.4.0

**Files:** `docs_site/versions.json`, `docs_site/src/content/versions/v0.4.0.json`, `docs/user/v0.4.0/**`

**Goal:** `v0.4.0` pinned docs buildable (no repeat of v0.3.2 missing-slug failure).

- [ ] Copy: `cp docs_site/src/content/versions/v0.3.2.json docs_site/src/content/versions/v0.4.0.json` (shape identical; verify `diff` shows 0 functional change). Append `versions.json` entry `{"slug":"v0.4.0","label":"v0.4.0","tag":"v0.4.0"}` preserving JSON formatting.
- [ ] Snapshot: `mkdir -p docs/user/v0.4.0 && cp -r docs/user/*.md docs/user/v0.4.0/ && cp -r docs/user/capabilities docs/user/concepts docs/user/v0.4.0/` excluding `docs/user/v0.*` self-copy (use `rsync --exclude 'v0.*'`). Then inject `slug:` frontmatter per file mirroring `v0.3.2` (`index.md` → `slug: v0.4.0`, `capabilities/index.md` → `slug: v0.4.0/capabilities`, others → `slug: v0.4.0/<relative-without-.md>`). Verify `grep -L "^slug:" docs/user/v0.4.0/**/*.md` → no output; `diff -r --brief docs/user/v0.3.2 docs/user/v0.4.0` shows only intended content drift.
- [ ] Verify: `cd docs_site && npm ci && npm run build` → exit 0, `dist/v0.4.0/` present (or at minimum `grep` + `astro build` green; if npm offline, record skip with reason + CI docs job as backstop). Commit: `docs(release): snapshot versioned docs v0.4.0`.

### Task 4: Gates + commit + oracle + PR + merge + tag

**Files:** none (process)

**Goal:** Green merge to `dev`, then `dev`→`main`, tag `v0.4.0`.

- [ ] Gates: `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run import-linter lint && uv run pytest -q` → all green; `uv run pytest --cov=paxman -q` + per-package `coverage report --fail-under=95`; all `tools/regenerate_*_data.py --check` green + `git diff --exit-code` clean for generated data.
- [ ] Oracle: run `paxman-oracle-review` on `git diff main...HEAD` (release-only diff) → must be PASS (blocking issues → fix + re-verify before PR).
- [ ] PR: `gh pr create --base dev --head chore/v0.4.0-release --title "chore(release): paxman 0.4.0 — minor (Element, Coordinates, ADR-0010/0011)"`. Wait 10 min, then `gh pr checks <n>` + `gh pr view <n> --comments` → address review/CI, repush, re-wait until green. Merge via `gh pr merge <n> --merge` (or squash per repo convention — check prior release PR merge method) only when CI passing.
- [ ] After PR merge: `git checkout main && git merge --ff-only origin/main && git merge dev --no-edit`, `git push origin main`, `git tag v0.4.0 && git push origin v0.4.0`. Verify publish workflow tag↔version parity (`pyproject.toml` `0.4.0` == `v0.4.0`). Post: `git checkout dev && git merge main`.

---

## Decisions

- Next version `0.4.0` (minor, not patch): 2 new caps (Element, Coordinates) + breaking (`Phone national` de-offer, `MacAddress bit_reversed` de-offer) per semver pre-1.0 minor rule. Patch `0.3.3` rejected — would understate breaking surface.
- Open `v0.4.0`-labelled audits (#137 Money, #135 MacAddress, #132 Language, #125 ISSN-followup, #116 ISBN-followup, #128/#127 Phone, #121/#120 Coordinates followups, #107/#106 followups, #73/#71 deferred, #45/#39/#15 deferred) are NOT release blockers — they are audits/followups/deferred, none is a P0 correctness regression on HEAD. Ship release on current HEAD; leave labels for `v0.4.1`/`v1.0.0` triage.
- PR base is `dev` (release branch from `dev`), then explicit `dev`→`main` + tag (matches user instruction; `versions.json` append lands on `main` via that merge before tag, satisfying versioned-docs D3b ordering).
