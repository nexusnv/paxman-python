# v0.6.0 Hardening Implementation Plan (#168, #169, #170, #154)

> **For workers:** Execute task-by-task via `tdd` + `verification-before-completion`. Review gate: `paxman-momus-review` (plan), `paxman-oracle-review` (after impl).

**Goal:** Ship v0.6.0 as a hardening minor — fix the intermittent e2e flake (#154), close out the #146 backlog with evidence, and land the Timezone Wave-2 first cut — with no contract, canonical-form, or engine changes.

**Architecture:** Two disjoint code tracks plus one no-code closeout. Track A (#154) is suite hygiene: repro the combined-run flake first, fix where the repro points (registry fixture, tmp-cwd, or hypothesis profile interaction). Track B (#170) is data-only: hand-curated additions to the four Timezone rule tables plus the grammar `NAME_TOKENS` mirror, pinned by the existing consistency tests. Nothing in `paxman/core`, `paxman/engine`, or any contract changes; #169 needs no branch (issue comments + branch deletions only).

**Tech Stack:** Python 3.11+, uv, ruff, strict pyright, import-linter, pytest (markers: capability/integration/property/e2e), hand-edited Timezone tables (NOT generated — no snapshot + generator script exists, per module headers).

**References:** #168 (parent), #169 (closeout), #170 (Wave-2), #154 (flake), #146 (backlog tracker), #164 (v1 Wave-2 backlog — stays open), `paxman/capabilities/Timezone/rules/data/iana_zone_identifiers.py:1-85`, `paxman/capabilities/Timezone/grammar/timezone_name_recognition.py:34`, `tests/capabilities/timezone/test_data_consistency.py:1-142`, `tests/e2e/test_cli_scan.py:42`, `docs/user/capabilities/timezone.md`, `CHANGELOG.md:9`, `CONTRIBUTING.md` § Branching and release.

**Branches:** `fix/suite-flake-154` (Track A), `feature/timezone-wave2` (Track B), both cut from `dev`. Track 0 needs no branch. PRs target `dev`, one per track. Promotion `dev` → `main` + tag `v0.6.0` on `main` HEAD happens only after all three close.

---

## File Structure

- Track 0 — no files. Ops only: `gh` comments + `git push origin --delete` × 6.
- Track A — Modify (after repro points): likely `tests/e2e/test_cli_scan.py:42` and/or suite-hygiene surface (`tests/conftest.py`, per-module registry fixture). Test: the repro loop itself + existing `tests/e2e/test_cli_scan.py`.
- Track B — Modify: `paxman/capabilities/Timezone/rules/data/iana_zone_identifiers.py:43-84`, `iana_zone_links.py`, `iana_fixed_zones.py`, `abbreviation_map.py` (only if a new carved/refused token is needed — default: untouched), `paxman/capabilities/Timezone/grammar/timezone_name_recognition.py:34` (`NAME_TOKENS` mirror). Test: `tests/capabilities/timezone/test_data_consistency.py`, `tests/capabilities/timezone/test_grammar.py`, `tests/integration/test_timezone_pipeline.py`, `tests/property/test_timezone_properties.py`. Docs: `docs/user/capabilities/timezone.md`, `CHANGELOG.md` (`## [Unreleased]` on top).

No `paxman/core` change. No generated-data regeneration (Timezone tables are hand-curated).

---

## Background the implementer needs

### Current state (verbatim)

`iana_zone_identifiers.py:18-25` documents the subset contract: ~30 well-known keys, SystemV gated, `backzone` out of scope, unlisted keys MISSING at the grammar never INVALID. `iana_zone_identifiers.py:31-38` gives the extension rule: add canonical-case key here (plus lowered source to `IANA_ZONE_LINKS` for Link targets); `test_data_consistency.py` must stay green — every link target in the set, no `^[A-Z]{2,5}$` key except `UTC`/`GMT`. The grammar keeps lowered keys-only in `NAME_TOKENS` and imports nothing from `rules` (separation is structural, asserted by `tests/unit/test_grammar_semantic_purity.py`).

### Design decisions (locked)

1. Wave-2 first cut = `Canada/Eastern` → `America/Toronto`, `US/PACIFIC` → `America/Los_Angeles`, `Etc/GMT-8` fixed zone, plus country-capital/US/Canada/Australia high-traffic links the implementer verifies against live `backward`/`zone1970.tab` (tzdb 2026d); each addition zoneinfo-probed like the v1 entries.
2. Subset contract unchanged: unlisted stays MISSING, short-caps carve rule enforced (`test_no_bare_short_caps_in_identifiers_except_utc_gmt`).
3. Flake fix must not weaken the hypothesis `ci` profile (`tests/conftest.py`) per `tests/AGENTS.md` anti-patterns; property tests stay off the registry.
4. #164 stays open as the v1 remainder; this track closes only #170.

---

### Track 0: #146 closeout — evidence, close, delete branches (#169, no branch)

**Files:** none (ops on `dev` HEAD).

**Goal:** Prove all 12 #146 rows dead, close #146, delete 6 dead remotes.

- [ ] Re-run each row's evidence command from its closing PR on current `dev` HEAD: `$500`+`MYR` → `INVALID` (#15/Slice F), `call me at` tail unclaimed vs `CALL ME AT` claimed (#106), `+14:30` bare → `INVALID` (#162-era), `Chinese (Traditional, Singapore)` → `SUCCESS zh-Hant-SG` (#147/#148), `America/Narnia` → `MISSING`, `0000000218250097` → `SUCCESS` (#39). Confirm `git rev-list --count dev..origin/<branch>` = 0 for all six branches, then `git push origin --delete` each: `audit/language-20260904`, `audit/macaddress-20260905`, `audit/money-20260906`, `feature/adr0012-candidate-qualification`, `fix/unicode-property-stage`, `feature/github-workflow-cron`. Post the table to #146 and close it; comment the branch deletions on #169 and close it. Verify: `gh issue view 146 --json state --jq .state` → `CLOSED`; `git branch -r | grep -E 'audit/|adr0012|unicode-property|github-workflow-cron'` → empty.

### Track A Task 1: Repro the flake (#154) — red loop first

**Files:** `tests/e2e/test_cli_scan.py:42`

**Goal:** Make `test_cli_scan_default_all_json` trip on demand in a combined run.

- [ ] Loop `uv run pytest tests/property tests/e2e/test_cli_scan.py -q` until the failure trips (Slice E precedent: 20× combined loops; count superseded loops toward the total honestly). Capture: failing seed/profile, whether isolation passes, registry/tmp-cwd suspects. Post the repro (command + iteration count + failure output) as a comment on #154. Expected: FAIL observed at least once with output showing which assertion tripped (returncode vs payload spans).

### Track A Task 2: Fix per repro finding — minimal change

**Files:** determined by Task 1 (candidates: `tests/e2e/test_cli_scan.py:42`, suite fixture, `tests/conftest.py` — profile itself must NOT be weakened).

**Goal:** Kill the ordering/shared-state cause without touching shipped behavior.

- [ ] Failing loop from Task 1 → implement the minimal isolation fix (e.g. per-test registry reset, cwd pinning, or subprocess env quarantine — whatever the repro implicates; shipped `paxman/` code is out of scope unless the repro proves a pipeline cause). Verify: the Task-1 loop runs 20 consecutive green iterations; `uv run pytest tests/e2e/test_cli_scan.py -q` → PASS. Commit on `fix/suite-flake-154`: `fix(tests): isolate <cause> for combined property+e2e runs (#154)`. Gate before PR: full quality suite green.

### Track B Task 1: Failing Wave-2 tests first (#170)

**Files:** `tests/capabilities/timezone/test_data_consistency.py`, `tests/integration/test_timezone_pipeline.py`

**Goal:** Pin the new keys red before touching tables.

- [ ] Add: consistency params for each new canonical key + link (`canada/eastern` → `America/Toronto`, `us/pacific` → `America/Los_Angeles`, `Etc/GMT-8` fixed) and pipeline tests `Canada/Eastern` → `SUCCESS America/Toronto`, `US/PACIFIC` → `SUCCESS America/Los_Angeles` under the default contract. Run: `uv run pytest tests/capabilities/timezone tests/integration/test_timezone_pipeline.py -q` → Expected: FAIL (keys absent from tables).

### Track B Task 2: Table expansion — data only

**Files:** `paxman/capabilities/Timezone/rules/data/iana_zone_identifiers.py:43-84`, `iana_zone_links.py`, `iana_fixed_zones.py`, `paxman/capabilities/Timezone/grammar/timezone_name_recognition.py:34`

**Goal:** Add the verified keys + lowered `NAME_TOKENS` mirror entries.

- [ ] For each key: verify against live tzdb 2026d `backward`/`zone1970.tab`, zoneinfo-probe it (v1 precedent: "probed OK 2026-09-14" comments), add canonical-case to identifiers (+ lowered link source where applicable, fixed-zone entry for `Etc/GMT-8`), add lowered mirror to `NAME_TOKENS`. Collision audit: new keys against `COMMON_WORDS` and the short-caps carve rule. Verify: Track-B Task-1 tests → PASS; `uv run pytest tests/capabilities/timezone -q` → PASS (consistency + grammar + rules + capability).

### Track B Task 3: Guides + changelog + full gate

**Files:** `docs/user/capabilities/timezone.md`, `CHANGELOG.md`

**Goal:** Document the new keys, record the minor behavior change, go green everywhere.

- [ ] Guide rows for each new key/link with executed-snippet output; `CHANGELOG.md` `## [Unreleased]` → `Added` entry (`MISSING` → `SUCCESS` per key, data-only, subset contract unchanged). Gate: `uv run ruff check . && uv run ruff format --check . && uv run pyright && uv run import-linter lint && uv run pytest --cov=paxman --cov-report=term-missing --tb=short -q && uv run coverage report --include="paxman/core/*,paxman/capabilities/*,paxman/engine/*,paxman/api/*" --fail-under=95` → all green. Commit on `feature/timezone-wave2`: `feat(timezone): Wave-2 first-cut table expansion (#170)`. Open PR against `dev`; close #170 on merge.

---

## Final verify (after all three close)

Full gate on `dev` HEAD + sustained combined `tests/property + tests/e2e/test_cli_scan.py` loop green (fold into Track A evidence). Promotion PR `dev` → `main` must leave the tagged commit gated: fast-forward `main` to the already-gated `dev` SHA when possible, otherwise rerun the full gate on the promotion merge commit before tagging. Then tag `v0.6.0` on the gated `main` HEAD, publish workflow, PyPI verify, GitHub Release — per `CONTRIBUTING.md` § Branching and release.
