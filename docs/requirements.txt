# Requirements for building docs with MkDocs (used by Read the Docs)
# See .readthedocs.yaml and mkdocs.yml
# Pin for reproducible builds — update with `uv pip compile` as needed

mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
pymdown-extensions>=10.0,<11
mkdocs-mermaid2-plugin>=1.1,<2
